<div class="inner-content loginWrp resumeWrp">
  <div class="container"> 
      <div class="row">
        <div class="col-md-9 col-sm-8">
          <% if @profile.any? %>
            <ul class="listService">
              <%= render @profile %>
            </ul>
            <%= paginate @profile %>
          <% else %>
            <h2><%= t "admin.profile.profile_not_found" %></h2>
          <% end %>
        </div>
      </div>
    </div>
</div>
